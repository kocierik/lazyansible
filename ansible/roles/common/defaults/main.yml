---
timezone: UTC
ops_user: ops
ssh_authorized_keys: []

common_packages:
  - curl
  - wget
  - git
  - vim
  - htop
  - unzip
  - jq
  - ca-certificates
  - gnupg

sysctl_params:
  net.ipv4.ip_forward: 0
  vm.swappiness: 10
  net.core.somaxconn: 1024
