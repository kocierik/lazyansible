# Local/dev inventory – no SSH, no sudo needed
[webservers]
localhost ansible_connection=local

[dbservers]
localhost ansible_connection=local

[all:vars]
app_env=development
ansible_python_interpreter=/usr/bin/python3
ansible_become=false
